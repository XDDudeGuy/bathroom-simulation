[package]
name = "bathroom_simulation"
version = "0.1.0"
authors = ["runner"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rand = "0.8.5"
memoize = {version="0.3.1", features=["full"]}
